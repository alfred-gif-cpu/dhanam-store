# Test-time only. The running server never imports these, so they stay out of
# requirements.txt and off the deployed image.
#
# Pinned for the same reason as the runtime ones: a test suite that installs
# a different pytest each week eventually fails for a reason that has nothing
# to do with the code, and a red check nobody trusts is a check nobody reads.
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
mongomock-motor==0.0.36
mongomock==4.3.0
