[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
# The app's own deprecation noise (FastAPI on_event, Pydantic class Config)
# would otherwise bury a real warning from the code under test.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
